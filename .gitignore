# VS Code specific files
.vscode/
*.code-workspace
.history/

# Common development hidden directories
.DS_Store
.env
.env.local
.cache/
.temp/
.tmp/
*.cache
*.m4
*.sh/
*.o
*.lo
*.a
*.Plo
*.Po
*.nsi
*.plist
*.in
*.dirstamp
*.la
*.guess
*.sub
*.so
*.so*
*.lai
lynxd
lynx-cli
lynx-util
lynx-tx
lynx-qt
lynx-wallet
Makefile

# Built binaries for any chain: the build passes 'make NAME=<chain>' so it emits
# <chain>d / <chain>-cli / <chain>-tx rather than the lynx* names above.
src/*d
src/*-cli
src/*-tx
src/*-qt
src/qt/res/icons/chain_icon.png

# Qt build artifacts (moc/uic/rcc/lrelease output), regenerated on every GUI build.
src/qt/*.moc
src/qt/moc_*.cpp
src/qt/forms/ui_*.h
src/qt/test/moc_*.cpp
src/qt/locale/*.qm
src/qt/qrc_*.cpp
src/qt/temp_bitcoin_locale.qrc
src/*-util
src/*-wallet

# Generated by configure from test/config.ini.in; holds absolute SRCDIR/BUILDDIR
# paths, so tracking it means every build from a different directory is a diff.
test/config.ini


src/config/stamp-h1
src/test/fuzz/fuzz
libbitcoinconsensus.pc
contrib/devtools/split-debug.sh
build-aux/ltmain.sh
libtool
configure~
configure
config.status
config.log
src/obj/build.h
.gitignore
doc/compile.sh
config.log
config.status
config.status
configure
libtool

src/obj/build.h
src/config/bitcoin-config.h
src/test/data/*.json.h
src/test/data/asmap.raw.h
src/test/test_lynx
ch
cpa
cps
gcae
vc
ce
cpc
eg
ga
gaf
gag
gst
hw
mw
tw
ve
vf
vw
src/.deps/
src/wallet/test/.deps/
deps/
src/test/.deps/
contrib/installer/MULTI_CHAIN_PLAN.md
contrib/installer/docs-spark-page.md
# Private chain-spec reference for unreleased chains. compile.sh no longer reads it —
# the buildable chain list comes from src/kernel/chainparams.cpp — and it must never be
# published, since it carries the parameters for chains that have not been released.
contrib/compiler/grunge.txt
